fn main() {
    // First phase:
    //
    // 1. creates the tun device: 10.0.0.100
    // 2. creates the udp socket:
    // 3. creates thread to read from tun and write to udp
    // 4. creates thread to read from udp and write to tun
}
